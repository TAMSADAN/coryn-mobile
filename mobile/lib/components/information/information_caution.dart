import 'package:flutter/material.dart';

class InformationCaution extends StatelessWidget {
  const InformationCaution({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("이 앱은 외부 사이트의 링크만을 모아 제공합니다. 해당 링크의 포함된 내용을 책임지지 않습니다.");
  }
}
