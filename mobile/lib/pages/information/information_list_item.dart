import 'package:flutter/material.dart';

class InformationListItem extends StatelessWidget {
  const InformationListItem({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
